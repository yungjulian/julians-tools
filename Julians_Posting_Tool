<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Julian's Posting Tool</title>

    <!-- Favicon hinzufügen -->
    <link rel="icon" href="https://www.rot-weiss-essen.de/wp-content/themes/RotWeissEssen/images/Rot-Weiss-Essen-Vereinslogo.png" type="image/png">

    <style>
        /* Grundfarben und Übergänge */
        body, .container {
            transition: background-color 0.3s, color 0.3s;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: flex-start; /* Fixiert das Tool oben */
            height: 100vh;
            position: relative;
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 500px;
            text-align: center;
            margin-top: 20px;
        }

        h2 {
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        h2 img {
            width: 30px; /* Größe des Logos */
            margin-right: 10px; /* Abstand zwischen Logo und Titel */
        }

        textarea {
            width: 100%;
            height: 120px;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 14px;
        }

        textarea:focus {
            border-color: #007bff;
            outline: none;
        }

        .button-container {
            display: flex;
            justify-content: space-around;
            margin-bottom: 15px;
        }

        button {
            background-color: #E1001E; /* Rot für Buttons */
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 14px;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        button img {
            width: 20px; /* Logo-Größe */
            margin-right: 8px; /* Abstand zwischen Logo und Text */
        }

        button:hover {
            background-color: #c1001a; /* Dunkleres Rot beim Hover */
        }

        #copyBtn {
            margin-top: 10px;
            background-color: #E1001E; /* Der rote "Text kopieren" Button */
        }

        #copyBtn:hover {
            background-color: #c1001a;
        }

        .toast {
            visibility: hidden;
            min-width: 250px;
            margin-top: 20px;
            background-color: #333;
            color: white;
            text-align: center;
            border-radius: 4px;
            padding: 10px;
            position: fixed;
            z-index: 1;
            left: 50%;
            bottom: 30px;
            transform: translateX(-50%);
            font-size: 14px;
        }

        .toast.show {
            visibility: visible;
            animation: fadeInOut 2s;
        }

        @keyframes fadeInOut {
            0% { opacity: 0; }
            50% { opacity: 1; }
            100% { opacity: 0; }
        }

        /* Ausgabe bleibt linksbündig */
        #outputField {
            text-align: left;
        }

        /* Darkmode Farben */
        body.dark-mode {
            background-color: #333;
            color: white;
        }

        body.dark-mode .container {
            background-color: #444;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }

        body.dark-mode button {
            background-color: #c1001a; /* Dunkleres Rot im Darkmode */
        }

        body.dark-mode button:hover {
            background-color: #e1001e; /* Helleres Rot im Darkmode */
        }

        body.dark-mode h2 {
            color: white; /* Helle Schrift für Darkmode */
        }

        /* Darkmode Button oben rechts */
        #darkModeToggle {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #E1001E; /* Roter Hintergrund für den Button */
            color: white;
            border: none;
            font-size: 16px;
            padding: 10px;
            border-radius: 4px;
            cursor: pointer;
        }

        #darkModeToggle:hover {
            background-color: #c1001a;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Überschrift mit Logo -->
        <h2>
            <img src="https://www.rot-weiss-essen.de/wp-content/themes/RotWeissEssen/images/Rot-Weiss-Essen-Vereinslogo.png" alt="Logo">
            Julian's Posting Tool
        </h2>

        <!-- Darkmode Toggle Button -->
        <button id="darkModeToggle" onclick="toggleDarkMode()">🌙</button>

        <!-- Eingabefeld -->
        <textarea id="inputField" placeholder="Gib hier deinen Text ein..."></textarea>

        <!-- Emoji Buttons -->
        <div class="button-container">
            <button onclick="insertEmoji('📲')">📲</button>
            <button onclick="insertEmoji('ℹ️')">ℹ️</button>
            <button onclick="insertEmoji('➡️')">➡️</button>
            <button onclick="insertEmoji('👉')">👉</button>
            <button onclick="insertEmoji('🔗')">🔗</button>
            <button onclick="insertEmoji('🎙')">🎙</button>
            <button onclick="insertEmoji('📺')">📺</button> <!-- Neuer Button für 📺 -->
        </div>

        <!-- Buttons für Instagram und Twitter mit Logos -->
        <div class="button-container">
            <button id="instagramBtn">
                <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/e/e7/Instagram_logo_2016.svg/2048px-Instagram_logo_2016.svg.png" alt="Instagram Logo"> Instagram
            </button>
            <button id="twitterBtn">
                <img src="https://abs.twimg.com/favicons/twitter.3.ico" alt="Twitter Logo"> Twitter
            </button>
        </div>

        <!-- Ausgabe Feld -->
        <textarea id="outputField" readonly placeholder="Hier erscheint dein formatierteter Text..."></textarea>

        <!-- Button zum Text kopieren -->
        <button id="copyBtn">Text kopieren</button>

        <!-- Toast Notification -->
        <div id="toast" class="toast">Text wurde kopiert!</div>
    </div>

    <script>
        function insertEmoji(emoji) {
            const inputField = document.getElementById('inputField');
            const cursorPosition = inputField.selectionStart;
            const textBefore = inputField.value.substring(0, cursorPosition);
            const textAfter = inputField.value.substring(cursorPosition);
            inputField.value = textBefore + emoji + ' ' + textAfter;  // Emoji mit Leerzeichen
            inputField.selectionStart = inputField.selectionEnd = cursorPosition + emoji.length + 1;  // Cursor nach Emoji + Leerzeichen
            inputField.focus();
        }

        document.getElementById('instagramBtn').addEventListener('click', function() {
            const inputText = document.getElementById('inputField').value;
            const instagramHashtags = "#rotweissessen #rwessen #rwe1907 #immeRWEiter #hafenstraße #nurderrwe";  // Hashtags ohne <p> Tags
            const formattedText = `${inputText}\n\n${instagramHashtags}`;  // Instagram Formatierung
            document.getElementById('outputField').value = formattedText;
        });

        document.getElementById('twitterBtn').addEventListener('click', function() {
            const inputText = document.getElementById('inputField').value;
            const twitterHashtags = "#immeRWEiter #nurderrwe";  // Hashtags ohne <p> Tags
            const formattedText = `${inputText}\n\n${twitterHashtags}`;  // Twitter Formatierung
            document.getElementById('outputField').value = formattedText;
        });

        document.getElementById('copyBtn').addEventListener('click', function() {
            const outputText = document.getElementById('outputField').value;
            navigator.clipboard.writeText(outputText).then(function() {
                // Zeige die Toast-Notification
                const toast = document.getElementById('toast');
                toast.className = "toast show";
                setTimeout(function() {
                    toast.className = toast.className.replace("show", "");
                }, 2000);
            }).catch(function(err) {
                console.error('Fehler beim Kopieren: ', err);
            });
        });

        function toggleDarkMode() {
            const body = document.body;
            const darkModeButton = document.getElementById('darkModeToggle');
            
            // Wechselt die Klasse für den Darkmode
            body.classList.toggle('dark-mode');
            
            // Ändert den Button Text je nach Zustand
            if (body.classList.contains('dark-mode')) {
                darkModeButton.innerHTML = '🌞'; // Sonne für den Rückwechsel
            } else {
                darkModeButton.innerHTML = '🌙'; // Mond für den Darkmode
            }
        }
    </script>
</body>
</html>
